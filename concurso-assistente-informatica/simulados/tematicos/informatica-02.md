# Simulado Temático — Informática 02

**Tempo:** 1h15 · **Questões:** 20 · **Alternativas:** A–E

Instruções: marque apenas uma alternativa. Foque só nesta matéria.

---

## Informática

**01.** O protocolo HTTP opera, por padrão, na camada de:

A) enlace, usando endereços MAC
B) aplicação, para transferência de páginas web
C) física, convertendo sinais elétricos
D) transporte, substituindo totalmente o TCP
E) rede, atribuindo IP automaticamente

**02.** O DNS (Domain Name System) tem função de:

A) filtrar spam de e-mail
B) traduzir nomes de domínio em endereços IP
C) criptografar arquivos locais
D) imprimir documentos em rede
E) compactar pacotes Wi-Fi

**03.** O serviço DHCP atribui automaticamente:

A) senhas de usuário
B) endereços IP e parâmetros de rede aos clientes
C) licenças do Microsoft Office
D) drivers de impressora
E) nomes de domínio internacionais

**04.** Um endereço IPv4 válido é:

A) 300.400.10.1
B) 192.168.0.10
C) 192.168.0
D) FF:EE:DD:CC:BB:AA
E) www.exemplo.gov.br

**05.** A diferença entre internet e intranet é que a intranet:

A) usa apenas satélites
B) é uma rede privada, geralmente interna à organização
C) não utiliza protocolo TCP/IP
D) substitui o DNS
E) funciona somente em smartphones

**06.** O protocolo usado para envio de e-mails entre servidores é:

A) FTP
B) SMTP
C) SNMP
D) Telnet
E) ICMP

**07.** Para receber e-mails em um cliente como Outlook, usa-se comumente:

A) SMTP para recebimento exclusivo
B) IMAP ou POP3 para recebimento
C) HTTP para exclusão de mensagens
D) DHCP para sincronizar calendário
E) DNS para anexos

**08.** Uma máscara de sub-rede 255.255.255.0 indica rede:

A) classe A exclusivamente
B) com 254 hosts utilizáveis em sub-rede /24 típica
C) sem gateway
D) somente IPv6
E) sem endereço broadcast

**09.** O cabo de par trançado categoria 6 (Cat6) é usado em:

A) redes Ethernet com maior desempenho que Cat5e
B) conexão exclusiva de monitores VGA
C) alimentação da CPU
D) impressoras matriciais somente
E) discos SCSI

**10.** O protocolo HTTPS adiciona ao HTTP:

A) compressão de imagens obrigatória
B) criptografia via TLS/SSL
C) endereçamento MAC
D) roteamento automático de VLAN
E) autenticação por DHCP

**11.** Um firewall de rede tem objetivo de:

A) aumentar a capacidade do HD
B) controlar e filtrar tráfego conforme regras de segurança
C) substituir o sistema operacional
D) editar documentos PDF
E) sincronizar relógio do BIOS

**12.** A porta padrão do protocolo FTP é:

A) 21
B) 25
C) 53
D) 80
E) 443

**13.** Wi-Fi refere-se a:

A) conexão por cabo coaxial
B) rede local sem fio baseada em padrões IEEE 802.11
C) protocolo de e-mail
D) sistema de arquivos Linux
E) impressão em nuvem

**14.** Um roteador atua principalmente:

A) amplificando sinal de áudio
B) encaminhando pacotes entre redes diferentes
C) armazenando backup em fita magnética
D) digitalizando documentos
E) formatando pendrives

**15.** O comando ping utiliza principalmente:

A) HTTP
B) ICMP para testar conectividade
C) SMTP
D) FTP
E) POP3

**16.** VPN (Virtual Private Network) permite:

A) aumentar resolução do monitor
B) criar túnel criptografado para acesso remoto seguro
C) substituir o DNS público
D) eliminar necessidade de senha
E) imprimir sem driver

**17.** Um endereço MAC identifica:

A) o site visitado no navegador
B) a interface de rede no nível de enlace
C) o servidor de e-mail
D) a pasta compartilhada
E) o usuário do Active Directory

**18.** Proxy server pode ser usado para:

A) soldar componentes da placa-mãe
B) intermediar requisições web e aplicar políticas de acesso
C) substituir memória RAM
D) calibrar monitores CRT
E) formatar HD automaticamente

**19.** Na rede, latência representa:

A) largura de banda máxima
B) tempo de resposta/atraso na comunicação
C) número de cabos instalados
D) potência do roteador
E) tamanho do arquivo anexo

**20.** O protocolo TCP garante, entre outros:

A) entrega confiável e ordenada de dados
B) endereçamento lógico de domínios
C) atribuição automática de IP
D) tradução de nomes DNS
E) criptografia de disco local
